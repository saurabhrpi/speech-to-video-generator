# Session Log

> **STICKY (do not remove):** Read Motto-and-Mantra.txt. ToDo's live in [ToDo.md](ToDo.md) — do not remove items unless user says. If you're ever unsure about ANYTHING, feel free to do web search, as many time as you like. If you get blocked doing web search by the system, just prompt me and I will approve it.

## Current Session: 40
**Date:** 2026-04-19
**Branch:** main (local ahead by mobile edits, not pushed)
**Status:** RevenueCat dashboard + SDK integration landed; Test Store working on simulator. Paywall UI (Task #6) is next.

## What Happened This Session

- **RC project + iOS app created.** Bundle `com.saurabh.speechtovideo`, `.p8` ASC API key uploaded. Both SDK keys captured: `appl_vmQCgcGmPddGaeaDkwiAOvJPaWt` (App Store) + `test_zQWpCAsWrKrLmsEhEVhFzZAUIwi` (Test Store).
- **Dashboard cleanup done.** Auto-generated Monthly/Yearly/Lifetime Test Store products replaced with a single consumable `pro`. Entitlement `Speech to Video Pro` and offering `default` (1 package `$rc_lifetime` → `pro`) wired and clean. Real `Speech to Video (App Store)` product list intentionally empty.
- **Mobile SDK wired.** `react-native-purchases@10.0.1` installed via `npx expo install` (no config plugin needed — autolinks). New `mobile/lib/purchases.ts` with `configurePurchases` / `syncPurchasesUser` / `resetPurchasesUser`. Integrated into `_layout.tsx` + `auth-store.ts`. `__DEV__` key switch (test in dev, appl in prod) after discovering Test Store needs its own SDK key.
- **Prebuild quirk resolved.** `npx expo prebuild` fails on missing Android `google-services.json`; `--platform ios` skips cleanly. Captured in new memory.
- **Product decisions locked (partial).** Credit packs only (no subs). Two tiers: Free (1 gen) + Pro (one consumable pack). **Pricing + gen count deferred** until paywall UX is visible.
- **LAUNCH_CHECKLIST.md #2 fully expanded** with progress markers + detailed plan for every remaining RC sub-step (paywall, precheck + clip-merge endpoints, entitlement gate, credit tracking, sandbox test).
- **Two new memories:** `reference_revenuecat_sdk_keys.md`, `reference_expo_prebuild_ios_only.md`.

## Next Step

**Task #6 — Build paywall UI with bundled Apple Sign In.** Before writing code, answer the three open questions from Session 39 handoff: (1) modal vs bottom-sheet presentation, (2) feature bullet copy, (3) trigger timing (auto-open on generate attempt vs manual button). Full implementation plan is in `LAUNCH_CHECKLIST.md` #2 under the ⏳ Task #6 bullet.

## Open Questions / Flags

- **Paywall presentation + copy + trigger** — three UX questions to answer at start of next session.
- **Lazy anon on fresh install** — still unresolved; may surprise RC `logIn` timing in the paywall flow. Worth a look before paywall lands. Now tracked in ToDo.md #15.
- **Credit-pack credit tracking** — net-new server-side concern surfaced while expanding Task #8. Not originally in the plan; need persistent balance (custom claim / Firestore) + RC webhook. ToDo.md #14 updated to reflect this.
- **Pro pack pricing** — deferred; revisit after paywall is on-screen.
- **Mobile code changes NOT pushed to origin yet.** `mobile/lib/constants.ts`, `mobile/lib/purchases.ts` (new), `mobile/app/_layout.tsx`, `mobile/store/auth-store.ts`, plus `mobile/ios/` regenerated by prebuild. Commit when paywall work starts.
- **Account deletion UI (Apple Guideline 5.1.1(v))** — still deferred; may block Beta App Review if reviewer flags. Required for any app with account creation.
- **4.3a carryover risk** — still deferred (ToDo.md #12). Relevant when submitting for review.
- **SESSION_SECRET Replit cleanup** — still deferred (inert either way).

## Session Artifacts

**Not yet committed (local only):**
- `mobile/lib/purchases.ts` (new)
- `mobile/lib/constants.ts` (RC keys added)
- `mobile/app/_layout.tsx` (`configurePurchases()` on mount)
- `mobile/store/auth-store.ts` (`syncPurchasesUser` / `resetPurchasesUser` hooks)
- `mobile/package.json` + `mobile/package-lock.json` (react-native-purchases@10.0.1)
- `mobile/ios/` regenerated by `expo prebuild --platform ios`
- `LAUNCH_CHECKLIST.md` (#2 expanded)
- `memory/MEMORY.md` + two new reference memories
