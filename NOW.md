# Session Log

> **STICKY (do not remove):** Read Motto-and-Mantra.txt. ToDo's live in [ToDo.md](ToDo.md) — do not remove items unless user says. If you're ever unsure about ANYTHING, feel free to do web search, as many time as you like. If you get blocked doing web search by the system, just prompt me and I will approve it.

## Current Session: 51 — 2026-04-24 — main (closing)
**Status:** Reds #1, #2, #3 from `LAUNCH_CHECKLIST.md` Submission Priority all DONE. Account deletion shipped + smoke-tested on sim. ASC IAPs + RC product wiring complete (Apple propagation pending, will verify naturally via TestFlight). Privacy/Terms backend live at `/privacy` and `/terms`. Two more Reds left before submission: #4 (UI polish) and #5 (App metadata in ASC).

## What happened this session

- **Red #1 — Account deletion: SHIPPED.** Backend `DELETE /api/account` (3 idempotent steps: credits doc → clip namespace → Firebase user; only Firebase-user failure raises 500). Helpers `credit_store.delete_ledger` + `clip_store.delete_namespace` (refuses empty arg so it can never wipe root). Mobile `auth-store.deleteAccount` (server-first while token valid, then gallery wipe + RC reset + Firebase signOut → onAuthStateChanged re-anons), `gallery-store.wipe` (aborts in-flight controllers + clears AsyncStorage), Settings Danger Zone + ConfirmModal. Dropped the redundant `router.back + setTimeout(400)` Buy Credits workaround (S49 paywall-Modal fix no longer needed since Paywall is now a root-level overlay). Tests #1 (anon) + #2 (signed-in) both PASS end-to-end on the wiped sim. Committed as `cea8065`.
- **Post-test UX fix on Account deletion.** User correctly flagged: anon users shouldn't see Delete Account — they have no user-facing "account" and the silent re-anon after delete looked like a no-op bug. Gated the Danger Zone card behind `!isAnonymous`. Apple 5.1.1(v) explicitly says "apps that support account creation must offer deletion" — anons don't create anything, so gating is compliant.
- **Red #2 — ASC IAPs + RC product swap: DONE (dashboard side).** Three Consumable IAPs created in ASC: `pro_pack_50` ($4.99 / 50), `pro_pack_120` ($9.99 / 120, BEST_VALUE), `pro_pack_250` ($19.99 / 250). All have localized names + descriptions, native simctl review screenshots at iPhone 17 Pro Max resolution (1320×2868), "All territories" availability, "Match parent app" tax. ASC In-App Purchase Key (.p8 + Key ID + Issuer ID) generated and uploaded to RevenueCat for receipt validation. Three App Store products created in RC under "Speech to Video (App Store)" app. Default offering's existing packages auto-linked the App Store products via shared identifiers (verified in Edit mode — each package shows both Test Store + App Store product attachments). `pay-as-you-go` Test Store package's App Store dropdown intentionally left empty (not in PACK_SKUS, won't be referenced in prod). "Current" is implicit since only one offering exists. Real-world purchase verification waits for TestFlight (Red #6).
- **Red #3 — Real Terms/Privacy URLs: DONE.** New `src/speech_to_video/api/legal.py` module ships Privacy Policy + Terms of Use as inline HTML at `/privacy` and `/terms`. Mobile-friendly CSS with light + dark mode. Content grounded in actual app behavior: anon Firebase UID, optional Apple Sign In + relay email, Whisper transcription with no audio retention, MiniMax/Kling generation, RC purchase metadata only, no analytics/ads/crash reporters/location/contacts/photos, in-app account deletion linked. Tennessee/Davidson County governing law, `support@speech-2-video.ai` contact (user confirmed live). Constants at module top for one-line edits. Backend redeployed; both endpoints verified live with `200` + correct content-type. Mobile constants already point at these URLs — no mobile rebuild needed.
- **Housekeeping.** Saved `Memory/reference_api_base.md` with both deployed URLs (canonical `speech-2-video.ai` + Replit alias `speech-to-video-generator.replit.app`) and fingerprint curl one-liners. Removed `QA/` from `.gitignore` (gitignore doesn't untrack already-committed files; user opted to keep tracking instead). Added `.claude/scheduled_tasks.lock` to `.gitignore` (Claude runtime artifact, not project state).

## Next step — Session 52 (on resume)

**Pick from the remaining Reds in `LAUNCH_CHECKLIST.md` Submission Priority:**

**1. Red #4 — UI polish pass.** Highest reviewer-perception lever, no dependencies. Scope is fuzzy without an audit — gut estimate 1–3 hours for a single-screen app at this maturity. Recommended approach: 15-min audit pass first (walk every screen on sim, enumerate concrete issues with severity), then triage in/out before touching code. Don't open-end this — submission is more important than perfection.

**2. Red #5 — App metadata in ASC.** More mechanical and directly unblocks submission. Scope: app description, keywords, support URL, marketing URL (optional), age rating questionnaire, App Store screenshots (the marketing ones — different from IAP review screenshots), App Privacy questionnaire (must mirror what `legal.py` declares: Firebase UID, Apple email, prompts/audio to AI providers, RC purchase metadata).

**3. Red #6 — Reviewer demo + TestFlight smoke.** Blocked on Red #4 + #5 first. Apple IAP propagation also takes hours — building TestFlight before that fully propagates wastes a build slot.

Suggested order: **#5 → #4 → #6** (mechanical first, polish second since polish can drift open-ended, TestFlight last). User can pick differently.

## Open questions (carryover + new)

- **(S51 new) Red #4 scope unsized.** Need 15-min audit before committing to it. UI polish can drift open-ended.
- **(S51 new) `pay-as-you-go` package in RC default offering.** Test Store-only, App Store dropdown intentionally empty. Cleanup opportunity (delete the package from offering) but not blocking; mobile `PACK_SKUS` doesn't reference it.
- **(S51 new) Apple IAP propagation pending.** Three IAPs in "Could not check" RC status until Apple ingests them (hours, sometimes a day). Not actionable — just wait. RC will auto-flip status once verified.
- **(S48 follow-up B, still open) UX hole: home button shows cost not balance.** `mobile/app/(tabs)/index.tsx` Generate button shows `Generate Video · N credits` (cost), not the user's balance. Decision needed on placement: chip above button, subtitle under header, or badge on gear icon. Not blocking submit.
- **S2V Product Vision** still blank in CLAUDE.md (`NEEDS USER INPUT`). Affects paywall copy, App Store description (Red #5), and reviewer perception. Should be pinned down before #5 lands cleanly.
- ~~**Kling COGS tuning**~~ — Resolved S52: Kling dropped entirely from V1. `CREDIT_COSTS` now has a single Hailuo 10s entry. Mobile `FALLBACK_COSTS` updated to match.
- **Backend Apple precheck + clip-merge** — Yellow #10. Haven't verified `/api/auth/apple/precheck` + `/api/clips/merge` exist in `server.py`. If missing, clip-orphan risk on anon→Apple collision.
- **(S48 carryover) Anon→Apple credit merge** — old anon UID's `credits/{uid}` doc orphans at link-time; new linked UID gets `starter_granted: false` and a fresh row. No harm in current bundled flow (sign-in always precedes purchase). If we ever allow purchase pre-sign-in, this needs cross-UID merge from Task #7.
- **(ToDo #1, S50 origin)** Server-side TOCTOU credit gate. Client mitigation already shipped (projected balance check), but server is still vulnerable to concurrent submits exhausting credits. Yellow priority — must land before TestFlight wider release, not blocking initial submission.
- **(S43-era, future trigger)** RC `default` offering "Current" is implicit today (only one offering exists, verified S51). **The day a second offering is added,** if `Purchases.getOfferings().current` returns `null`, check the RC dashboard for an explicit "Current" toggle on the offering you want served.
- **(S49 carryover)** iCloud on sim flaky on hang — if it recurs persistently, move anon-path QA to physical device via TestFlight.
