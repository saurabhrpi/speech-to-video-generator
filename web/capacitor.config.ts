import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.saurabh.interiortimelapse',
  appName: 'Interior Timelapse',
  webDir: 'dist'
};

export default config;
