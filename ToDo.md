# ToDo's

> Do not remove items unless user says.
> Numbered IDs are stable (referenced from `LAUNCH_CHECKLIST.md`); ordering reflects priority, not ID.

23. **Remove "· N credits" suffix from Generate Video button label** (S52, MUST LAND IN RED #4 POLISH BEFORE SUBMISSION). Current `mobile/app/(tabs)/index.tsx` button title is `Generate Video · 10 credits`. User feedback: ambiguous — reads as "10 credits is your balance" OR "this video costs 10 credits." Change to just `Generate Video`. If we ever want to surface per-gen cost, do it as a subtle hint elsewhere (e.g., under the divider, or in the empty-state placeholder), NOT as a suffix on the CTA. Affects the `cost !== null ? \`Generate Video · ${cost} credits\` : 'Generate Video'` line.

24. **Exclude EU/EEA at submission time** (S52, MUST DO AT SUBMISSION). App Store Connect → your app → **Pricing and Availability → Availability** → pick **"Selected Countries or Regions"** and uncheck all EU/EEA countries: Austria, Belgium, Bulgaria, Croatia, Cyprus, Czech Republic, Denmark, Estonia, Finland, France, Germany, Greece, Hungary, Ireland, Italy, Latvia, Lithuania, Luxembourg, Malta, Netherlands, Poland, Portugal, Romania, Slovakia, Slovenia, Spain, Sweden + Iceland, Liechtenstein, Norway. Removes the DSA paperwork requirement for V1. Re-enable EU markets in a future version once we either (a) incorporate and have a registered business address, or (b) get a virtual mailbox for trader info. Without one of those, EU listing publicly exposes home address + phone.

1. **Server-side concurrent-submit credit gate (revenue leak)** (S50). `_check_credits_or_402` in `src/speech_to_video/api/server.py:141-154` is a non-atomic read-then-allow; `_maybe_consume_job_credits:610-642` deducts atomically only at job completion. Race: 3 concurrent submits with 5 anon credits → check passes for all 3 (balance still 5), all 3 jobs run, only first deduction succeeds, subsequent two log `"shortfall at completion"` and the user gets free videos. **MiniMax can't backstop us** — S50 research confirmed no sub-account / per-user quota / programmatic key APIs (`https://platform.minimax.io/docs`). Mobile-side projected-balance check (item 20 / S50) closes the honest-user case; this server fix closes the adversarial-curl case. **Must land before TestFlight wider release.** Fix options: (A) cheap — at submit, count this uid's in-flight jobs and reject if any (one-`if` server diff, no schema change, preserves current "consume-on-completion" guarantee for crash resilience); (B) reservation pattern — atomic `reserve_for_job` at submit, `settle` on completion, `release` on failure (new Firestore field `reservations`, more code, robust across restarts); (C) simplest — switch to consume-at-submit + refund-on-failure (loses the "server restart doesn't lose credit" guarantee, since Replit restarts are common per `memory/reference_replit_startup_event.md`). Default to A unless we want to also harden accounting; revisit if we ever migrate to AIMLAPI or fal.ai (their per-user keys could obviate this entirely).
2. **Recovery attempt for the two lost videos (Session 31).** MiniMax `download_url` TTL is 1 hour — by now almost certainly expired. Task IDs: `387451605938268`, `387453794079043`. Worth checking if MiniMax client has a "fetch-by-task-id" helper that bypasses the TTL, otherwise close this out. Besides: a.) if we add SSE recovery, do we also need server-side cancel-on-client-disconnect? (Separate concern — cost control when user truly abandons).
b.) Push notification on completion as a v2 improvement (real fix for "user walks away for 20 min").
3. **Test the SSE-kill fix.** Refer to Test_plan.md for more details. (a) Simulator: `npx expo run:ios` → Test 2 (toggle Mac Wi-Fi off for ~50s mid-job → card flips to paused/cloud-offline → Wi-Fi on → auto-resume → completes), Test 3 (pause state + force-quit + relaunch → `hydrate()` resumes), Test 5 (happy path regression — progress updates every ~5s now, was sub-second under SSE). (b) Device: EAS Build → TestFlight → Test 1 (FaceTime mid-job).
4. **Invite friend as internal tester** via App Store Connect (Users and Access → Developer role → Interior Timelapse only; then TestFlight → Internal Testing group). Blocked by #3.
5. **Apply the same SSE → polling fix to the timelapse pipeline** (`mobile/store/pipeline-store.ts` — two `streamJob` call sites at lines 94 and 174). Same class of iOS-background bug; just hasn't surfaced because we haven't tested timelapse while backgrounded. After this, `mobile/lib/streaming.ts` can be deleted.
6. Confirm Session 29 dev-menu-reload crash root cause. Reproduce, retrieve `.ips` from `~/Library/Logs/DiagnosticReports`, verify reanimated frames in stack trace.
7. ~~Re-test with Kling model (only Hailuo tested so far).~~ **OBSOLETE (S52)** — Kling dropped from V1. Re-add to ToDo only if Kling is restored post-launch.
8. Test the recorded-audio path (only typed-prompt tested so far).
9. Simulator paste keeps breaking. `simpaste` alias is a workaround, not a fix. Find a permanent solution.
10. Fix Button.tsx type error (`mobile/components/Button.tsx:87`). Pre-existing, non-blocking — app runs fine — but `tsc --noEmit` is noisy and `<Button>` loses autocomplete. One-line fix: replace `require('react-native').Pressable` with a top-level `import { Pressable }` so `createAnimatedComponent` can infer prop types.
11. Local `expo run:ios --device` still blocked by Session-31 keychain issue (`errSecInternalComponent` when codesigning React.framework). EAS Build works fine; simulator works fine; only physical-device dev builds are broken. Low priority — EAS is the working path for device testing. See `Memory/reference_keychain_reset_recovery.md` for what was already tried.
12. **Flaky NetInfo auto-resume in simulator.** Test 2 run 1: paused card stuck for 2+ min after Wi-Fi restored (only recovered on app background→foreground via AppState). Run 2: recovered in seconds via NetInfo. Root cause suspected: `wasOffline` gate in `mobile/app/_layout.tsx` NetInfo listener — if the simulator doesn't emit an offline event when Mac Wi-Fi drops, `wasOffline` stays `false` and the online event is ignored. Fix: drop the `wasOffline` gate, call `resumePausedJobs()` on every NetInfo online event (idempotent — no-op if no paused jobs, skips jobs with an active AbortController).
13. **Fix App Store Review rejection (Guideline 4.3a - Spam).** Apple rejected the Interior Timelapse build, saying it looks like a repackaged template. To pass review on Speech to Video: (a) add real App Store metadata — screenshots, compelling description, keywords; (b) polish the UI so it doesn't look like a starter template; (c) ensure the reviewer can actually generate a video — may need a demo mode or test credentials in review notes.
14. **Investigate CLI-based EAS submission monitoring.** Session 37 attempted `eas submit:list` and `eas submission:view` — neither exists. User recalls past sessions may have monitored submit via CLI. Worth confirming: is there an undocumented or newer EAS CLI command to poll submission status by ID? Or is the only path the web URL + email notification? Low priority — emails work fine.
15. **IAP Credit-Balance Gating (replaces `tech_debt.md`).** Current `src/speech_to_video/api/server.py` gates only anonymous users via `UNAUTH_GEN_LIMIT`; signed-in users are unrestricted. Violates the monetization model in `memory/project_monetization_model.md` — sign-in must lead to payment, not unlimited free gens. **Session 43 decisions locked** (see `LAUNCH_CHECKLIST.md` Task #2 sub-step #8 for the full detail — this is the short version):
    - **Credits = currency.** 1 credit = $0.10 retail, ~2x markup over COGS. Per-gen cost: Hailuo 6s=5, 10s=9 | Kling 3s=5, 10s=15, 15s=23.
    - **Packs:** `pro_pack_50` $4.99, `pro_pack_120` $9.99, `pro_pack_250` $19.99 (rename freely in RC later).
    - **Anon free tier = 5 credits** auto-granted on first API call. Replaces `UNAUTH_GEN_LIMIT`.
    - **Verification: RC REST.** `POST /api/credits/grant` hits `api.revenuecat.com/v1/subscribers/{uid}` with `REVENUECAT_REST_API_KEY` to verify the purchase before granting. No webhook.
    - **Storage: Firestore** (collection `credits`, doc id = UID) via new `utils/credit_store.py`. Reuses `firebase-admin` SDK already in use for auth. Atomic grant/consume via `FieldValue.increment()` in transactions; idempotency on `applied_transactions` array. Well within Firestore free tier. (Session 44 reversal of the Session 43 filesystem-first plan — no migration debt.)
    - **Gate:** `balance >= cost(model, duration)` → 402 on shortfall. Anon + signed-in share the same path. `/api/auth/session` returns `{credit_balance, cost_table}`.
    - **Files touched:** new `utils/credit_store.py`, new `api/credits.py`, `api/server.py`, `mobile/lib/constants.ts`, `mobile/store/auth-store.ts`, `mobile/components/Paywall.tsx`, `mobile/app/(tabs)/index.tsx`, `mobile/app/settings.tsx`.
16. **Add a confirmation prompt before deleting the video from gallery when user presses X on the card/thumbnail.**
17. **`<NetworkBanner />` hidden behind Paywall** (S49 Case 1a). Originally diagnosed as iOS Modal-window stacking; after S50 refactor of Paywall to root-level `Animated.View` overlay (`zIndex: 1000`), root-level NetworkBanner still renders BEFORE Paywall in the layout tree → still covered. Fix: render `<NetworkBanner />` inside `Paywall`'s SafeAreaView, OR give NetworkBanner a higher z-index than Paywall (1001+) so it floats above when offline.
18. **Restore silently closes paywall on offline failure** (S49 Case 1a). `Paywall.handleRestore` awaits `restoreAndGrant` + `refreshCredits`; both swallow errors internally. Try block always "succeeds" offline → `closePaywall()` runs → paywall slides down with zero feedback. Fix: surface offline/network errors from `restoreAndGrant` (either rethrow or return a success/failure signal), and/or check `NetInfo.fetch()` before attempting restore. Note Test Store also returns empty silently (`Restoring purchases not available in Test Store`), so we also need to distinguish "offline" from "nothing to restore" from "restore succeeded, no matching txs" (see also item 19).
19. **No `CustomerInfo` listener — offline-replay + RC-ingestion-lag gap** (S49 Case 1a, originally surfaced S48). Our app calls `/api/credits/grant` only from `Paywall.handlePurchase` (when `purchasePackage` resolves) and `Paywall.handleRestore` (manual tap). Real App Store `StoreKit.PaymentQueue` replays transactions that failed mid-flight (offline, crash, etc.) via its observer API, and RC emits these through `CustomerInfo` updates. Without a listener, replayed transactions are silently lost until the user manually taps Restore. Fix: add `Purchases.addCustomerInfoUpdateListener(info => ...)` in `configurePurchases()` (`mobile/lib/purchases.ts`) that diffs `nonSubscriptionTransactions` against a locally-tracked set of already-granted txIds and calls `grantCreditsForTransaction` (idempotent server-side) for each new matching tx. Also closes the S48 "RC Test Store REST delay" gap (`memory/reference_rc_test_store_rest_delay.md`) — auto-grants after ingestion catches up instead of requiring Restore tap.
20. **Generate-button silent-disable is a poor UX** (S50). Current projected-balance check (`mobile/app/(tabs)/index.tsx` `blockedByInFlight` flag) just disables the button while an in-flight job would push us over budget. User has no inline feedback explaining *why* — no "Wait for current generation". Better UX options: (a) inline helper text under the CTA ("Generating… (next: N credits)"); (b) repurpose the disabled-button title to "Generating, please wait"; (c) just queue the next gen instead of blocking. Pick after we've watched real users hit it.
21. **Story-continuation prompt nudge (enhancement)** (S50). After a clip is generated (or in parallel during gen), suggest a "next-scene" prompt that continues the narrative of the user's last prompt — letting them stitch a multi-clip story without having to think up each beat. Open questions: (a) timing — pre-submit autocomplete under the input, or post-completion suggestion on the gallery card? (b) source — cheap GPT-4o-mini call vs in-house template? cost matters since most users may ignore the suggestion; (c) presentation — single suggestion, carousel of 2-3, or "Surprise me" button? (d) does the user want narrative continuity, visual continuity, or both? — visual continuity probably needs I2V with previous clip's last frame as control (out of scope of "shipping" S2V pipeline; would need an I2V toggle). Start with a cheap text-only suggestion under the prompt box; layer richer modes later.
22. **V2 quality-bar enhancements (post-launch)** (S52, from Vision interview Topic 3). Three improvements to gen quality/UX, all out of scope for V1 launch:
    (a) **Auto-detect abstract/failed output and silently retry once.** Detection signal TBD — cheapest start is frame-to-frame variance over the returned .mp4 (a clip with near-zero motion is almost always a failure). Avoids charging for completely-broken gens.
    (b) ~~**Best-of-2 on Kling only.**~~ **OBSOLETE (S52)** — Kling dropped from V1. Re-frame: if any future premium-tier model lands, that's where best-of-N applies (margin headroom to absorb 2x model cost). Hailuo alone can't absorb best-of-N at current pricing.
    (c) **Free regen on detected hard-fails** (paired with #a's detection). When auto-detect fires AND the silent retry also fails, regenerate at zero credit cost. Trust-builder for the quality bar.
    V1 ships single-shot with a manual full-cost regenerate button — see launch-checklist Red #4 polish items.
26. **Decide whether to keep paying for the M365 / Entra tenant for `support@speech-2-video.ai`** (S53). Discovered S53: a Microsoft 365 subscription was provisioned via the registrar (GoDaddy or similar — primary domain prefix is `NETORGFT...`, the reseller-tenant pattern) when the `.ai` domain was bought. Tenant `speech-2-video ai` (Tenant ID `0a610013-4754-...`), 3 mailboxes (support@, admin@, etc.), all owned by the user as Global Admin. **Sole use today:** hosting `support@speech-2-video.ai` so the App Store support URL works. **Cost:** ~$6/mo per mailbox. **Cheaper alternatives if migrating:** (a) **Cloudflare Email Routing** — free, forward to personal Gmail; (b) **Forward Email** — ~$3/yr, supports reply-as via SMTP relay; (c) **Zoho Mail** — free for 1 domain / 5 users, full mailbox; (d) **iCloud+ Custom Domain** — included with iCloud+, lands in iCloud Mail. **Migration steps if dropping M365:** change MX records at the registrar to the chosen forwarder → verify mail flow for support@ → cancel the M365 subscription via the registrar → delete the Entra tenant (Entra admin → Properties → Manage tenant; requires removing all subscriptions/users/apps first). **If keeping it:** ensure MFA is enabled on the Global Admin account (S53 secure score 73.68% suggests partial MFA coverage); the "do not allow user consent" recommendation was applied S53.
