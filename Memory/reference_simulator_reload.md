---
name: Simulator reload shortcut
description: Use Ctrl+Cmd+Z to open dev menu in iOS simulator, not Cmd+D (no longer works)
type: reference
---

To reload the app or open the React Native dev menu in the iOS simulator, use **Ctrl+Cmd+Z** (shake gesture). Cmd+D no longer works.
